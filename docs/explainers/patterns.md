# Stud-placement patterns

> Auto-generated from `src/open_studs/params.py`.

## Working

- **`single_row`** — evenly-spaced studs along the centerline. Default spacing 12 mm.
- **`double_row`** — two parallel rows. Default spacing 12 mm.
- **`staggered`** — two rows offset by half a step. Default spacing 12 mm. Classic punk look.

## Stubs

- **`cluster`** — small groups with gaps between. Signature locked, topology TBD.
- **`gradient_size`** — varying stud sizes along the band. Needs signature extension.
